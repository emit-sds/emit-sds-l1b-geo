# Note I'm replaced this functionality for now with the config file from
# the l1_osp_dir. Leave this in place short term, in case we go back to this.
# It isn't 100% where we should be getting the pge name, version, etc.

# This contains the version information for each of the build_id, build_version,
# and pge_version. This file is loaded by each of the PGEs, so you can
# edit this information in one place through this file.

import os

pge_version = {"l1b_geo" : "0.01",
               }

    
